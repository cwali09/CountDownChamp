import React from 'react';
import ReactDOM from 'react-dom'; //allows you to throw code onto the browser
import App from './App.jsx'

ReactDOM.render(
  <App />, //grabs the root id in the index.html file
  document.getElementById('root')

);
